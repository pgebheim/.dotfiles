# Legacy vim-plug checkouts + nvim-generated caches (undo history, tree-sitter
# parser binaries/queries). ~/.vim now symlinks to nvim/, not here; nothing
# under vim/ has ever been tracked in git.
vim/
