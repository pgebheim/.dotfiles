# Per-machine tmux overrides (untracked template lives in local/omarchy/).
# Intentionally empty: the powerline themepack statusline (shared tmux.conf)
# owns the statusline on all machines. Omarchy window/pane tinting is applied
# at runtime by omarchy-theme-set-tmux — nothing needed here.
